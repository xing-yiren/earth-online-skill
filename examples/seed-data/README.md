# Seed Data

该目录保存地球Online 的示例种子数据。

用途：

- 初始化本地 `runtime/examples/seed-data/`
- 支持 `scripts/smoke_test.py`
- 为外部读者提供可理解的示例状态

说明：

- 这里是示例数据，不是实际用户运行时存档
- 真实运行时状态应落在 `runtime/examples/seed-data/`
